//Primitive type variables are string, number, boolean, undefined and null. Their values is saved into the variable itself

let firstName = 'Amel' // String Literal
let age = 30 // Number Literal
let isApproved = false; // Boolean Literal
let lastName = undefined;
let selectedColor = null;

//There are also Reference type variables whose values are not stored in the actual variable